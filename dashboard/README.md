# Dashboard Streamlit

Painel administrativo para supervisores acompanharem operacao, produtividade, ocorrencias e relatorios.

## Executar

```bash
streamlit run dashboard/app.py
```

Configure a API em `API_BASE_URL` se necessario:

```bash
set API_BASE_URL=http://127.0.0.1:8000
```
